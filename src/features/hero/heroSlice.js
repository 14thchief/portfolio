import { createSlice } from "@reduxjs/toolkit";

const initialState = {}
const reducers = {};
export default createSlice({
  name: 'hero',
  initialState,
  reducers,
  extraReducers: (builder) => {
  },
});